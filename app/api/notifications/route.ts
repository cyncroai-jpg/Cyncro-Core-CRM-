import { cleanText, coreDb, ensureCoreSchema, hasModuleAccess } from "@/lib/core/db";
export async function GET(request: Request) { try { await ensureCoreSchema(); if(!(await hasModuleAccess(request,"calendar")))return Response.json({error:"Calendar access is required."},{status:403}); const recipient=cleanText(new URL(request.url).searchParams.get("recipient"),160); const {results}=recipient?await coreDb().prepare("SELECT * FROM workspace_notifications WHERE recipient=? ORDER BY created_at DESC LIMIT 50").bind(recipient).all():await coreDb().prepare("SELECT * FROM workspace_notifications ORDER BY created_at DESC LIMIT 50").all(); return Response.json({notifications:results}); } catch(error){console.error("notifications.list_failed",error);return Response.json({error:"Unable to load notifications."},{status:500});} }
export async function PATCH(request: Request) { try { await ensureCoreSchema(); if(!(await hasModuleAccess(request,"calendar")))return Response.json({error:"Calendar access is required."},{status:403}); const body=await request.json() as Record<string,unknown>;const id=cleanText(body.id,80);if(!id)return Response.json({error:"Notification id is required."},{status:400});await coreDb().prepare("UPDATE workspace_notifications SET read_at=? WHERE id=?").bind(new Date().toISOString(),id).run();return Response.json({saved:true}); } catch(error){console.error("notifications.update_failed",error);return Response.json({error:"Unable to update notification."},{status:500});} }
